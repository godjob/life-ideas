# 2026-03-23

## 気づき・学び

### sui-memory：Claude Codeの長期記憶システムの設計判断
- noprogllamaの記事を検証。自分のlifeリポジトリ（CLAUDE.md + auto memory）との比較で有効性を評価
- **参考になる設計判断**:
  1. 記憶保存にLLMを使わない（要約による情報損失を回避）→ 生ログをチャンク保存し、検索時に選別する方が実用的
  2. FTS5（固有名詞）+ ベクトル検索（意味的近さ）のハイブリッド → 単一検索方式の限界を超える
  3. 時間減衰（半減期30日）→ 古い記憶のスコアを自然に下げる仕組み
  4. session_end Hookで自動保存 → 手動操作ゼロの記憶蓄積
- **lifeリポジトリへの導入は見送り**: 現在は定型作業（git scrap, git books）が主体で壁打ち頻度が限定的。sentence-transformers（PyTorch）の依存が重い。macOS 11互換性リスクあり
- **将来の検討ポイント**: 壁打ちの頻度が増えた時、session_end Hookで `/reflect` を自動実行するだけでも記憶の自動蓄積に近い効果が得られる可能性

### 書籍メタデータ補完の完了
- fill_book_meta.py を作成。OpenBD→NDL Search API→Google Booksの3段階フォールバックで170件中132件を補完
- import_kindle.pyの再実行時メタデータ消失バグも修正済み（load_existing_meta関数）
